# 2026-03-30

## Observed issues
- Flipper motor max force is extremely high, which can inject unrealistic energy into the ball and make rebounds/spins unstable.
- Flippers have no angular velocity cap, so rapid motor reversals can spike angular speed and stress joint limits.

## Changes
- Added flipper angular velocity limiting and reduced motor max force to curb energy blow-ups.
- Added a headless test that simulates flipper actuation and asserts the limit/angle bounds.

## Parameter diffs
- `FLIPPER_MAX_FORCE`: (new) 300000
- `FLIPPER_ANGULAR_VELOCITY_LIMIT`: (new) 25

## Test results
- `python3 -m unittest discover -s tests -p "test_*.py"` → **failed** (ModuleNotFoundError: pymunk)

## Next steps
- Install dependencies in the test environment (`pip install -r requirements.txt`).
- If flippers still feel too weak/strong, tune `FLIPPER_MAX_FORCE` in small increments.
- If tunneling appears at high speeds, consider light sub-stepping or higher `Space.iterations`.
