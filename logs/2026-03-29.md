# 2026-03-29

## Observed issues
- Ball speed can spike after strong launcher/flipper interactions, risking tunneling through thin walls and unstable bounces.
- No explicit velocity cap to keep Chipmunk integration stable at high impulses.

## Changes
- Added a max ball speed cap via `Body.velocity_limit` on ball creation.
- Added a headless test to assert the velocity cap clamps large impulses.

## Parameter diffs
- `MAX_BALL_SPEED`: (new) 2200

## Test results
- `python3 -m unittest discover -s tests -p "test_*.py"` → **failed** (ModuleNotFoundError: pymunk)

## Next steps
- Ensure test environment installs dependencies (`pip install -r requirements.txt`).
- If tunneling still appears, consider slight sub-stepping or a modest increase in `Space.iterations`.
- Revisit flipper motor force if the cap is hit too often in play.
