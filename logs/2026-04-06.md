# 2026-04-06

## Observed issues
- The ball can accumulate extreme spin from repeated high-friction contacts (flippers/walls), which can destabilize collision response at high speeds.
- There was no automated check confirming angular velocity limits are enforced to prevent spin blow-ups.

## Changes
- Added a ball angular velocity limit in settings and applied it in `create_ball` to cap extreme spin.
- Added a headless test that asserts the ball’s angular velocity cap is respected after a step.

## Parameter diffs
- `BALL_ANGULAR_VELOCITY_LIMIT`: new setting at `200`

## Tests
- `python3 -m unittest discover -s tests -q` → **FAILED** (missing dependency `pymunk` in environment).

## Next steps
- Install `pymunk` in the test environment so physics tests can run headless.
- If spin still feels too strong/weak during play, tune `BALL_ANGULAR_VELOCITY_LIMIT` (e.g., 150–250) and re-check the new angular velocity test.
