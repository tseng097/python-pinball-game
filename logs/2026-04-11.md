# 2026-04-11

## Observed issues
- Flippers initialize at the default body angle (0) even when the intended rest angle is different (especially the right flipper). This causes an initial snap as constraints pull the flipper into position, injecting unwanted energy at game start.
- No automated coverage verifying that both flippers spawn at their rest angles.
- Headless tests require pymunk to run; current environment lacks pymunk, so automated checks fail to import.

## Changes
- Set flipper body angle and angular velocity at construction to match the configured rest angle, preventing the startup snap.
- Added a headless test to assert both flippers spawn at their rest angles and remain within limits after a short step.

## Parameter diffs
- No parameter changes.

## Tests
- `python3 -m unittest discover -s tests -q` → **FAILED** (ModuleNotFoundError: No module named 'pymunk').

## Next steps
- Install pymunk in the test environment so headless physics tests can run.
- Consider adding a small sleep/settle step after reset to ensure stable initial state if future constraints are added.
