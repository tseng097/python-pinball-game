# 2026-04-12

## Observed issues
- Flipper release still applies motor torque (via max_force), which can fight the return spring and add extra energy/oscillation when the flipper is supposed to be passive.
- No automated check confirms the flipper motor fully disengages on release.

## Changes
- Set flipper motor max_force to 0 when inactive so the spring alone handles the return motion.
- Extended the flipper motor idle test to assert the motor force is fully disengaged when released.

## Parameter diffs
- No parameter changes.

## Tests
- `python3 -m unittest discover -s tests -q` → **FAILED** (ModuleNotFoundError: No module named 'pymunk').

## Next steps
- Install pymunk in the test environment so headless physics checks can run.
- Validate that flipper return speed remains acceptable with the motor fully disengaged (tune spring if needed).
