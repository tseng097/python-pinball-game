# 2026-03-31

## Observed issues
- Physics stepping is single-step per frame; high-speed impacts can tunnel through thin walls or feel unstable.
- Space configuration (iterations/substeps) is not centralized, making tests diverge from runtime settings.

## Changes
- Added `src/physics.py` with `configure_space()` and `step_space()` to centralize physics configuration and sub-stepped integration.
- Increased solver iterations and added substeps for better collision stability.
- Updated game loop and physics tests to use shared space config + stepping helper.
- Added a fast-ball wall containment test to catch tunneling regressions.

## Parameter diffs
- `SPACE_ITERATIONS`: (new) `20`
- `PHYSICS_SUBSTEPS`: (new) `2`

## Test results
- `python3 -m unittest discover -s tests -q` → **FAILED** (ImportError: `pymunk` not installed in environment).

## Next steps
- Consider tuning `PHYSICS_SUBSTEPS` vs. performance once pymunk is available for headless test runs.
- Review flipper motor rates if gameplay still feels overly energetic after stability changes.
