# 2026-04-04

## Observed issues
- Flipper return relied solely on the rotary spring; `down_speed` existed but was unused, so release behavior could feel sluggish and inconsistent under load.
- No explicit automated check that the flipper moves back toward rest shortly after release.

## Changes
- Use the configured `down_speed` to actively drive the flipper back toward rest on release (small motor assist).
- Added a headless test that verifies the flipper angle moves toward its rest position shortly after release.

## Parameter diffs
- `Flipper.set_active(False)` motor rate: `0.0` → `down_speed` (with reduced max force still at 20%).

## Tests
- `python3 -m unittest discover -s tests -q` → **FAILED** (missing dependency `pymunk` in environment).

## Next steps
- Install test dependencies in CI/runtime (`pymunk`) so headless physics tests can run.
- Monitor for any overpowered return behavior; if needed, reduce `down_speed` or motor force when inactive.
