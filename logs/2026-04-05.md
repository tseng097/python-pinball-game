# 2026-04-05

## Observed issues
- Wall elasticity was fairly high (0.9), which can let the ball retain too much energy and occasionally feel “floaty” after repeated wall bounces.
- No automated check ensured repeated wall bounces don’t increase ball energy.

## Changes
- Introduced configurable wall/drain elasticity values and lowered wall elasticity to reduce energy retention on rebounds.
- Added a headless test that simulates repeated wall bounces with gravity off and asserts no energy gain.

## Parameter diffs
- `WALL_ELASTICITY`: `0.9` → `0.85` (new setting)
- `DRAIN_ELASTICITY`: new setting at `0.7` (matches prior hard-coded value)

## Tests
- `python3 -m unittest discover -s tests -q` → **FAILED** (missing dependency `pymunk` in environment).

## Next steps
- Install `pymunk` in the test environment so physics tests can run headless.
- If bounces feel too “dead,” revisit `WALL_ELASTICITY` (e.g., 0.87–0.9) and re-check the energy test.
