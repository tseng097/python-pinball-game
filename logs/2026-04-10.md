# 2026-04-10 Pinball Physics Daily Log

## Observed issues
- Flipper motors drive continuously into rotary limit joints, which can cause jitter/energy injection near the limit and makes the flippers feel “buzzy” at rest.
- Flippers do not explicitly idle once they are at their rest or max angles; motor keeps applying torque even when already at the target.
- Could not execute tests locally because `python` is not available in this environment.

## Changes
- Added a small flipper limit buffer and stop the motor when the flipper is at/near the rest or max angle, reducing torque against the limit joint.
- Added a headless automated test to assert the motor idles near limits after simulated steps.

## Parameter diffs
- `FLIPPER_LIMIT_BUFFER`: added `0.02` (new)

## Tests
- `python -m unittest -q` (failed to run: `python` not found in environment)

## Next steps
- Run the full unittest suite in an environment with Python to confirm the new test passes.
- If flipper response feels too soft at the top end, consider reducing the buffer slightly (e.g., 0.01) while keeping the motor idle behavior.
