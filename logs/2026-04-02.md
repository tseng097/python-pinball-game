# 2026-04-02

## Observed issues
- Fast-moving balls can still tunnel or clip through wall segments when the frame dt is near the max clamp (substeps were low at 2).
- Large impulses (launcher/flippers) combined with high elasticity can amplify this tunneling risk at lower substep counts.
- Automated coverage did not explicitly stress a large-dt, high-speed case.

## Changes
- Increased physics substeps to improve continuous collision handling at larger dt values.
- Added a headless regression test that simulates a very fast ball at the clamped dt and asserts it remains within bounds.

## Parameter diffs
- `PHYSICS_SUBSTEPS`: `2` → `3`.

## Test results
- `python3 -m unittest discover -s tests -p "test_*.py" -q` → **FAILED** (ImportError: `pymunk` not installed in environment).

## Next steps
- Install `pymunk` in the dev/CI environment so physics tests can run.
- If tunneling persists at extreme speeds, consider one more substep or reducing `PHYSICS_MAX_DT` slightly.
